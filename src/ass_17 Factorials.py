# Create a function which takes one positive integer as its input and returns its factorial.
# To make sure that your function works correctly,
# you should call it with the inputs 3, 4, and 5 and
# print what is returned by those calls.  For those inputs,
# you should get 6, 24, and 120, respectively.

n = t = 5
y = n - 1
f = 0

# def fac_tor (x):
while (n > 1):
        # t = t * (t - 1)
    t = f = t * y # f = 20 * 4
    # t = f       # t = 20
    print(f ," << factorial --- n> ",  n, t, "t" , y , "y")
    n -= 1
    y = n - 1


# //  ----------------------------------
# //  do with other method


